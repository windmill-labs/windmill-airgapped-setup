description: Square OAuth token
schema:
  $schema: 'https://json-schema.org/draft/2020-12/schema'
  type: object
  properties:
    token:
      type: string
      description: Square OAuth token
      default: ''
  required:
    - token
